<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="TB_BLG_CONT">
    <select id="insert" parameterType="hashMap" resultType="int">
        <![CDATA[
        insert into blog.tb_blg_cont
            (title, cont_bdy)
        values (#{title}, #{content_body})
        returning blg_cont_seq
        ]]>
    </select>

    <select id="selectOne" parameterType="java.lang.Integer" resultType="hashMap">
        <![CDATA[
        select blg_cont_seq,
               title,
               cont_bdy,
               insert_dt
        from blog.tb_blg_cont
        where blg_cont_seq = #{blogContSeq}
        ]]>
    </select>
</mapper>
